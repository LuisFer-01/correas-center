import ListFooters from './ListFooters'
import CreateFooter from './CreateFooter'
import EditFooter from './EditFooter'
const Pages = {
    ListFooters: Object.assign(ListFooters, ListFooters),
CreateFooter: Object.assign(CreateFooter, CreateFooter),
EditFooter: Object.assign(EditFooter, EditFooter),
}

export default Pages