import ListRegistros from './ListRegistros'
import CreateRegistro from './CreateRegistro'
import EditRegistro from './EditRegistro'
const Pages = {
    ListRegistros: Object.assign(ListRegistros, ListRegistros),
CreateRegistro: Object.assign(CreateRegistro, CreateRegistro),
EditRegistro: Object.assign(EditRegistro, EditRegistro),
}

export default Pages